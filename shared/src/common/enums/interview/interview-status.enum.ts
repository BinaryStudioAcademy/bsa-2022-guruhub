enum InterviewStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in-progress',
  COMPLETED = 'completed',
  REJECTED = 'rejected',
  NEW = 'new',
  CANCELED = 'canceled',
}

export { InterviewStatus };
