enum ApiPath {
  AUTH = '/auth',
  PERMISSIONS = '/permissions',
  GROUPS = '/groups',
  USERS = '/users',
  CATEGORIES = '/categories',
  COURSES = '/courses',
  COURSES_MODULES = '/courses',
}

export { ApiPath };
