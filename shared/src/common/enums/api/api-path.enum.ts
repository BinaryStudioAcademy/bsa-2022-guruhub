enum ApiPath {
  AUTH = '/auth',
  PERMISSIONS = '/permissions',
  GROUPS = '/groups',
  USERS = '/users',
  CATEGORIES = '/categories',
  COURSES = '/courses',
  COURSES_$ID_MODULES = '/courses/:courseId/modules',
}

export { ApiPath };
