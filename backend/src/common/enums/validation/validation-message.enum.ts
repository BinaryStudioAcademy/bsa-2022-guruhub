enum ValidationMessage {
  EMAIL_ALREADY_EXISTS = 'Email is already taken',
  BAD_CREDENTIALS = 'You have entered an invalid username or password',
  DELETE_NOT_FOUND = 'User to delete not found',
}

export { ValidationMessage };
