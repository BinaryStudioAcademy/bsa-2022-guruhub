get:
  summary: Get All Chat Messages
  tags:
    - chatMessages
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: '../../schemas/chat-messages/chat-get-all-messages-response-dto.yaml'
