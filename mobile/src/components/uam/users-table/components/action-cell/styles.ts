import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actionWrapper: {
    width: 30,
    height: 30,
    marginHorizontal: 10,
    justifyContent: 'center',
  },
  icon: {
    margin: 8,
  },
});

export { styles };
