import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingLeft: 10,
    flexDirection: 'row',
  },
  button: {
    marginRight: 15,
  },
});

export { styles };
