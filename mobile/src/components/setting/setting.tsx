import React, { FC } from 'react';

import { Text, View } from '~/components/common/common';

const Settings: FC = () => {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
};

export { Settings };
