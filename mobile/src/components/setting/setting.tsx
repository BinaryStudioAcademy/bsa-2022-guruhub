import React, { FC } from 'react';
import { Text, View } from 'react-native';

const Settings: FC = () => {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
};

export { Settings };
