import React, { FC } from 'react';
import { View, Text } from 'react-native';

const Settings: FC = () => {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
};

export { Settings };
