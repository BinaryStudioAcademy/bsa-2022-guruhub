import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    justifyContent: 'center',
    height: 30,
    paddingLeft: 10,
    paddingRight: 20,
    marginTop: 34,
    position: 'absolute',
    top: 0,
    right: 0,
  },
});

export { styles };
