import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // marginTop: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { styles };
