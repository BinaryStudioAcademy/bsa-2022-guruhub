import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  button: {
    paddingVertical: 8,
  },
  saveButton: {
    marginLeft: 10,
  },
});

export { styles };
