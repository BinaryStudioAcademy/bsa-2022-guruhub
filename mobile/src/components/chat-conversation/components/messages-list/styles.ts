import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    alignItems: 'center',
  },
  messageList: {
    minWidth: '100%',
    flexDirection: 'column-reverse',
  },
});

export { styles };
