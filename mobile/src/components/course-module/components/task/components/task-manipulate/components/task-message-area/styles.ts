import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    marginTop: 10,
    borderRadius: 10,
    marginBottom: 20,
  },
  buttonContainer: {
    marginTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    marginBottom: 20,
    width: 200,
  },
});

export { styles };
