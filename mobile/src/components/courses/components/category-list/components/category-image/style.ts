import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoWrapper: {
    width: 30,
    height: 30,
    padding: 7,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { styles };
