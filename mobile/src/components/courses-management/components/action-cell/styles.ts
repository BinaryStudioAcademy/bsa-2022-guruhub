import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actionWrapper: {
    width: '100%',
    height: 40,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { styles };
