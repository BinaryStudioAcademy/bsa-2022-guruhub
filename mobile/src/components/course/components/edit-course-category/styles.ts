import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    paddingHorizontal: 10,
  },
  saveButtonContainer: {
    alignSelf: 'center',
    marginTop: 20,
    width: 150,
  },
});

export { styles };
