import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  saveIconContainer: {
    marginTop: 10,
    marginRight: 10,
  },
});

export { styles };
