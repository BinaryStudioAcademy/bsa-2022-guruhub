import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 16,
    paddingBottom: 0,
  },
  noStudents: {
    marginLeft: 15,
    fontSize: 16,
    color: 'white',
  },
});

export { styles };
