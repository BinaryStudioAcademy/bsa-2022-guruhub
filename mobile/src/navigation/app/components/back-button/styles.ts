import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backButton: {
    borderColor: 'white',
    width: 10,
    height: 10,
    margin: 20,
    borderLeftWidth: 2,
    borderTopWidth: 2,
    transform: [{ rotate: '-45deg' }],
  },
});

export { styles };
