import { FC } from 'common/types/types';
// import { useParams } from 'hooks/hooks';

const StudentsCourses: FC = () => {
  // const { studentId, courseId } = useParams();

  return <div></div>;
};

export { StudentsCourses };
